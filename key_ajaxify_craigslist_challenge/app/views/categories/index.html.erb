<h1>Categories</h1>

<% @categories.each do |category| %>
<h3>
  <%= link_to category.name, category_articles_path(category) %><br>
</h3>
<% end %>
</ul>

<h3 id="goal-2">Goal: Display view of the link on the current page (on the click of a category)</h3>
